import {
  AesCryptor,
  CryptoProvider,
  Errors,
  Hash,
  LogLevel,
} from '@ridi/parser-core';

import Book from './model/Book';
import ComicParser from './ComicParser';

export default {
  AesCryptor,
  ComicParser,
  ComicBook: Book,
  CryptoProvider,
  Errors,
  Hash,
  LogLevel,
};
